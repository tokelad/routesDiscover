# OSM Map Frontend

Basic React project structure based on Vite.

# Create planetiler map

```
java -Xmx8g -jar planetiler.jar \
  --download \
  --osm-path=../zaragoza.osm.pbf \
  --output=zaragoza.mbtiles
```

## Run

```bash
npm install
npm run dev
```
